-- Change the default Omarchy look'n'feel.

hl.config({
  general = {
    -- No gaps between windows or borders.
    gaps_in = 0,
    gaps_out = 0,
    border_size = 0,
  },

  decoration = {
    blur = {
      enabled = false,
    },
  },

  animations = {
    enabled = false,
  },
})
